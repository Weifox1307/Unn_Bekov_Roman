Товары
- код товара
- название
- цена
- единица измерения

Клиенты
- код клиента
- фамилия
- имя
- отчество
- адрес
- телефон
- e-mail
- признак постоянного клиента (прописать условие: если приобретение товаров достигает суммы свыше 5000 р -> переход в категорию "постоянный клиент" (unique category) + скидка на каждую покупку в размере 2%)

Продажи
- код продажи
- код товара
- код клиента
- дата продажи
- дата доставки
- количество

Общие связи между таблицами:

-> Параметр "Код товара" есть в таблицах "Товары" и "Продажи"
-> Параметр "Код клиента" есть в таблицах "Клиенты" и "Продажи"

==========
Код таблиц
==========

CREATE TABLE IF NOT EXISTS tovary_data (
 id SERIAL PRIMARY KEY,
 name_tovar STRING NOT NULL,
 tovar_price INT NOT NULL,
 tovar_count INT NOT NULL
);

CREATE TABLE IF NOT EXISTS client_data (
 id_client SERIAL PRIMARY KEY,
 surname STRING NOT NULL,
 name STRING NOT NULL,
 lastname STRING NOT NULL,
 address TEXT NOT NULL,
 telephone_number TEXT NOT NULL,
 email TEXT NOT NULL,
 